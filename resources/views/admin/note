<x-app title="Dashboard" bodyClass="w-full h-full">
    <div class="items-center w-full h-full">
        <!-- Filter Buttons -->
        <div class="px-6 pt-3 flex gap-3">
            <form action="{{ route('dashboard') }}" method="GET">
                <button
                    type="submit"
                    name="filter"
                    value="month"
                    class="drop-shadow-lg bg-blue-500 text-white px-4 py-2 rounded"
                >
                    Bulan
                </button>
                <button
                    type="submit"
                    name="filter"
                    value="year"
                    class="drop-shadow-lg bg-blue-500 text-white px-4 py-2 rounded"
                >
                    Tahun
                </button>
                <button
                    type="submit"
                    name="filter"
                    value="all"
                    class="drop-shadow-lg bg-blue-500 text-white px-4 py-2 rounded"
                >
                    All Time
                </button>
            </form>
        </div>

        <!-- Periode Info -->
        <div class="px-6 pt-3">
            <h3 class="text-lg font-semibold">Periode: {{ $periode }}</h3>
        </div>

        <!-- Cards Section -->
        <div class="px-6">
            <div class="flex flex-row w-full gap-4 pt-2 h-44">
                <!-- Sampah Dominan -->
                <div
                    class="h-full bg-white rounded-lg drop-shadow-lg border w-full"
                >
                    <div class="p-2 text-2xl flex flex-col h-full">
                        <label>Sampah Dominan:</label>
                        <label
                            class="text-4xl"
                            >{{ $currentWaste->category->name ?? 'Tidak Ada' }}</label
                        >
                        <label
                            >Total =
                            {{ $currentWaste->total_weight ?? 0 }} kg</label
                        >
                    </div>
                </div>

                <!-- TPS Dominan -->
                <div
                    class="h-full bg-white rounded-lg drop-shadow-lg border w-full"
                >
                    <div class="p-2 text-2xl flex flex-col h-full">
                        <label>TPS Dominan:</label>
                        <label
                            class="text-4xl"
                            >{{ $currentTPS->landfill->name ?? 'Tidak Ada' }}</label
                        >
                    </div>
                </div>

                <!-- Kurir Teladan -->
                <div
                    class="h-full bg-white rounded-lg drop-shadow-lg border w-full"
                >
                    <div class="p-2 text-2xl flex flex-col h-full">
                        <label>Kurir Teladan:</label>
                        <label
                            class="text-4xl"
                            >{{ $topCourier->courier->name ?? 'Tidak Ada' }}</label
                        >
                    </div>
                </div>

                <!-- Pendapatan -->
                <div
                    class="h-full bg-white rounded-lg drop-shadow-lg border w-full"
                >
                    <div class="p-2 text-2xl flex flex-col h-full">
                        <label>Pendapatan:</label>
                        <label class="text-4xl"
                            >Rp.
                            {{
                                number_format($currentRevenue, 0, ",", ".")
                            }}</label
                        >
                    </div>
                </div>
            </div>
        </div>

        <!-- Overview -->
        <div class="w-full mt-3">
            <h3 class="text-lg font-semibold">Overview</h3>
            <div class="h-52 rounded-lg shadow-md border p-4">
                {{ $overview }}
            </div>
        </div>
    </div>
</x-app>
